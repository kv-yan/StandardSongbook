package am.betel.songbook.common.presentation.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily

val FontRegular = FontFamily(
    Font(
        resId = am.betel.songbook.R.font.grapalat_reg,
    )
)

val FontBold = FontFamily(
    Font(
        resId = am.betel.songbook.R.font.grapalat_bold,
    )
)

val FontBoldItalic = FontFamily(
    Font(
        resId = am.betel.songbook.R.font.grapalat_blit,
    )
)

val FontItalic = FontFamily(
    Font(
        resId = am.betel.songbook.R.font.grapalat_rit,
    )
)